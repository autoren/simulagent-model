# V165r1 outcome-verifier repair results

V165r1 confirmed the sole mismatch was the run-level `population_build_count: 1` field added through a shared
Python access-dictionary reference. The persisted embedded scientific audit differs from independent reconstruction
only at that field and is exactly equal after the prospectively locked projection removes it. The separate result
access record retains build count 1.

All V165 population assets, factors, version spaces, metrics, gates, safety boundaries, and the positive scientific
decision remain unchanged. The original failed audit and nominal V165 outcome absence are preserved. V165r1 did
not rebuild or rewrite the population and used no evaluation data, model, API, training, ontology registration,
service, side effect, action, or execution.

This repair is not new scientific evidence. It permits the positive V165 result to be retained and authorizes only
separate preregistration of V166 model-free deterministic baselines.
