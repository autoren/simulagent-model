# V148 typed semantic-witness firewall results

## Verdict

V148 is a positive model-free feasibility result. The typed witness firewall makes the optional LLM proposal causally irrelevant to acceptance while preserving operational known, generic novel-candidate, unsupported, and insufficient-evidence states.

All 98 valid-witness-by-proposal cases were exact. Candidate invariance, known acceptance precision and recall, generic novel-candidate routing, unsupported routing, insufficient-evidence routing, deterministic output validity, and authoritative hypothesis retention were each 100%. All ten malformed witnesses failed closed to `A00`. All twelve one-field near-known mutations avoided known acceptance even when the LLM proposal named that known capability.

No language was read and no model was loaded, generated, or scored. There were zero API calls, training runs, capability registrations, actions, or executions.

This is not language-understanding evidence. The result assumes that a trusted closed interaction supplies the typed witness; acquiring that witness from free-form language remains the unresolved empirical problem. The generic `N00` route proposes no schema or mechanic and is non-executable.

Freeze the decision:

`freeze_typed_witness_firewall_feasible_authorize_fresh_interaction_population_design_only`

The only authorized successor is model-free design of a wholly new blind interaction population in which clarification answers are closed and independently checkable. A separate preregistration would still be required before any local model access. APIs, training, calibration fitting, capability induction, authority, action, and execution remain closed.
