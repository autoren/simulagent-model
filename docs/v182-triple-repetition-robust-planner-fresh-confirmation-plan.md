# V182 triple-repetition robust-planner fresh-confirmation plan

## Objective

Evaluate the unchanged V180 mechanism once on every frozen V181 eligible state and target. The estimand is fresh
evidence-context generalization inside the same finite ontology under one possible globally corrupted raw observation.

Each measurement block repeats one binary inspection three times, decodes by strict majority, and costs `0.3`. The
planner may use at most four blocks. Trusted routing remains restricted to unanimous alias or composition version
spaces; mixed and unanimously provisional spaces defer at loss two.

## Controls and gates

Retain the seven V180 policies unchanged: immediate deferral, the V175 clean policy repriced at triple cost, greedy
information gain, best fixed open loop, random block order, exact robust adaptive certification, and a non-operational
target-informed certificate oracle. Use all 66 states and all 528 targets with frozen class-balanced weights.

Reconstruct every score, exact dynamic-programming root, target certificate, corruption-route invariant, and sandbox
transaction independently. Safety, primary confirmation, and strong-control dominance remain separate. Any safe
negative or mixed result is frozen without tuning.

## Boundary

V182 tests only the fixed symbolic observation code and ontology. Planner, oracle, hidden target, model, and
provisional primitive have no commit authority. No model, API, real sensor or service, registration, trusted real-state
mutation, external side effect, or execution is permitted.
