# V200 transformed `CHAR_LAST` controls plan

V200 evaluates the unchanged V194 `CHAR_LAST` comparator on the two exact V199 development-menu variants. It is
preregistered before reading or scoring V192 language in this role.

Every predicted option ID is mapped through the record-specific hidden bijection before comparison. This makes task
accuracy and paired invariance contract-level quantities rather than string-ID quantities.

`ORDER_ONLY` must reproduce canonical `CHAR_LAST` exactly: top-1 contract agreement and mean top-3 contract-set
Jaccard must be one, target-hit disagreement zero, primary top-3 recall `0.8541666667`, and cost `0.2583333333`.
The opaque-ID variant may alter deterministic tie-breaking; it must retain at least `0.75` primary and macro top-3
recall to remain an informative transformed comparator.

Exactly 98 development records may be read and 2,352 option scores computed. Missing records are not scored and map
to insufficient. Manual inspection, protected access, model/API/training use, ontology mutation, services, side
effects, action, and execution remain zero. A pass authorizes only a separately preregistered unchanged local-model
development robustness experiment.

