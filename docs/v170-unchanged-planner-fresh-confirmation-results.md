# V170 unchanged planner fresh confirmation results

## Outcome

V170 passed every integrity gate and every preregistered strong-confirmation threshold. The unchanged V167
Bayes-adaptive planner was evaluated once on all 58 V169 states whose frozen version spaces contained all three
ontology classes. None of those constraint signatures appeared in V165's ambiguous development population.

Mean exact expected total risk was:

| Policy | Mean risk |
|---|---:|
| Oracle class | 0.0000 |
| Exact Bayes-adaptive | 1.3229 |
| Optimal open-loop pair | 1.3432 |
| Greedy class information gain | 1.3466 |
| Random open-loop pair | 1.6716 |
| Immediate Bayes terminal decision | 2.0000 |
| Forced MAP without defer | 3.3333 |

All 58 states had positive value of information. Exact adaptive planning strictly improved over the optimal
fixed query pair on all 58. The corrected V167r1 action projection found history-dependent second actions in 49
states. Exact Bayes root queries varied across valuation indices 0, 1, 2, and 7, so no single query dominated the
fresh population.

Every state retained exactly 64 candidates and all three expressibility classes. Eligible-population coverage,
exact risk reconstruction, and pointwise Bayes dominance over every non-oracle comparator were all 100%.

## Interpretation

This is a fresh procedural confirmation of the mechanism developed in V167. The prior, query cost, horizon,
loss matrix, policy set, and corrected history-dependence metric were frozen before any aggregate V170 policy
score was opened. Population membership was frozen separately in V169 without using policy or risk scores.

The result strengthens the conclusion that retained ontology uncertainty can support useful adaptive evidence
gathering. Its advantage is not tied to the 48 V165 development evidence patterns: it reproduced across every
eligible, nonoverlapping two-constraint state in the complete V169 enumeration.

The confirmation remains inside the same project-authored finite Boolean DSL. It is not external transfer,
unrestricted ontology learning, natural-language understanding, trusted registration, state authority, action,
or execution evidence.

## Boundary and decision

No model, API, training, evaluation record, manual judgment, ontology registration, trusted-state mutation,
service call, side effect, real action, or execution occurred. Beliefs, queries, and decisions remained
shadow-only.

Freeze V170 as a strong nonoverlapping procedural planner confirmation. Advance next to a separately locked
stateful-sequence confirmation of V168's fixed-ontology reversible sandbox. Do not design cross-track integration
until that second mechanism also survives its fresh confirmation.
