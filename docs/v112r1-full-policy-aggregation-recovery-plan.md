# V112r1 Mechanical Aggregation Recovery Plan

V112 completed all 240 preregistered generations, then failed before producing any metric because the
ask-always comparator helper was called without its intentionally unused positional argument. The durable
census preserved all 240 raw fixtures and the exact access record.

V112r1 freezes the failure artifact and a SHA-256 manifest of every preserved fixture before aggregation.
It changes one mechanical call only: the current record is passed to `ask_always_prediction(record)`. It
reuses the exact V112 population, extracted language, parsed model outputs, retrieval representation,
policy, calibration constants, comparators, metrics, gates, and decision rule. It performs no model load or
generation and permits no manual language or raw-response inspection.

This recovery can validate or reject V112's preregistered hypothesis, but cannot broaden its authorization.
Protected-test access, schema induction, sequential planning, APIs, training, action authority, and execution
remain closed.
