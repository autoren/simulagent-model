# V220 fresh Mondo artifact population plan

V220 is the separately locked payload successor authorized by the verified-positive V219A census. It uses exactly the
untouched Mondo pair `v2026-05-05 -> v2026-06-02` and the eight asset roles frozen in the V219A outcome lock.

Before any payload byte is read, the design audit must establish exact parent authority, URL/role/size/digest equality
with V219A, a closed 97 MB budget, one attempt per asset, no unlisted network request, and no import resolution. The
official newer-release API body already frozen by V217A/V219A is the release-summary control; no README or additional
API request may substitute for it.

The population reconstructs normalized asserted states only. Stable IDs and asserted `replaced_by`/`consider` links
define concept families. Each eligible family receives version-unspecified and current-release-declared records, exact
candidate state classes, expressibility state, boundary witnesses, a correct decision, and decision consequences.
Public text is identifier-redacted. All linked records remain in one deterministic development/protected group.

The population must pass direct noncompensatory strata: at least 24 addition families, four text-change families,
three lifecycle families, 12 mapping families, 12 ambiguous version-unspecified families, and 12 decision-contrast
families, alongside the inherited integrity and oracle gates. These thresholds are frozen from the independent release
metadata and the scientific minimums before retrieval; no stratum may compensate for another.

Protected files may be structurally created and hashed but cannot be manually inspected or used for method evaluation.
V218 development/protected records and V216/V213 protected data remain sealed. V220 runs no candidate method or model
and authorizes no ontology registration, trusted mutation, service action, or execution.

