# V190 protected multiway menu-compression confirmation plan

## Purpose

V189 formally failed because its control gate was incompatible with the observed robust-value branch. V190 does not repair or rerun V189. It tests the unexpected development hypothesis on the previously sealed protected identity distribution.

## Frozen policy

Ask the declared domain, then—only when the domain contains multiple contracts—ask the normalized intent concept within that domain. Stop early on singleton domains. Price each categorical answer at `0.10 * ceil(log2(k))`, where `k` is the number of reachable categories at that history. This is the zero-overhead conservative bit-slot rule.

The sequence is fixed before protected target scoring. No policy optimization, target-informed selection, or protected prior adaptation is allowed.

## Controls and gates

Compare with generic trusted clarification at 0.40, a flat 14-intent menu at 0.40, and domain-only followed by generic clarification. All observed paths must be typed-only exact, retain the target, cost at most 0.395 on average, improve over generic by at least 0.005, and average at most 1.90 turns. The protected cost must remain within 0.02 of V189's development cost. Missing controls remain insufficient at zero cost.

Passing confirms only hierarchical menu compression in a finite oracle-answer universe. It does not establish adaptive planning, human usability, language understanding, or model reliability. Protected utterance language remains unread.
