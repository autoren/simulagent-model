# V84 Schema-Grounded Shadow Results

V84 passed every frozen gate. The generic model-free interface accepted all four valid typed schemas
and rendered all 108 schema cases with strict validity and exact typed-request preservation. Coverage
included all three fresh transfer schemas.

All 12 invalid-schema mutations were rejected, all 13 unknown or malformed requests failed closed,
and all 16 unsafe surface mutations were rejected. Every valid-looking untrusted surface remained
non-deployable. No fallback guessed a schema, slot, action, or question.

The separate V79 bridge covered all six reachable clarification nodes in all nine rendering modes,
for 54 cases. Action preservation and policy-node structural preservation were both `1.0`; the maximum
policy-value error was `0.0`, and complete-belief execution-certificate violations remained zero.

V84 used zero model loads, generations, API calls, training runs, human records, original user
language, real tool calls, and external side effects. The positive result authorizes bounded synthetic
shadow use and preregistration only—not execution—of a later local model as a non-deployable offline
adversarial-input generator.
