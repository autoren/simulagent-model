# V149 fresh closed-interaction population results

## Verdict

V149 is a positive population-design result. It freezes 288 project-authored controlled interaction fixtures in 48 complete six-stage groups, split evenly between development and evaluation. Four declared known capabilities, generic `N00`, forbidden `U40`, and insufficient `A00` are represented across six known-versus-novel-or-unsupported families.

All 96 closed-answer events map through six immutable two-option questions to exact typed witnesses and then to the correct firewall state. All 192 pre-answer records remain fail-closed at `A00`, and all eight malformed answer-event classes fail closed. Every registered query option maps exactly, the complete authoritative hypothesis universe is retained, and no capability is defined, registered, or executed.

Public and hidden fixture IDs align exactly, public rows contain no hidden truth fields, every group is complete and belongs to one split, and exact conversation overlap with V135, V142, and V146 is zero. No model, API, training, action, or execution occurred.

This remains synthetic population evidence. It does not demonstrate that an LLM recognizes the correct mechanic or chooses the correct question. The evaluation partition is project-authored and must not be described as external transfer.

Freeze the decision:

`freeze_fresh_closed_interaction_population_authorize_oracle_policy_only`

The next authorized step is an exhaustive model-free oracle interaction policy establishing query value, final witness routing, and decision costs. No local model run, calibration, induction, authority, action, or execution is yet authorized.
